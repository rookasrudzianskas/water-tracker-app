# Water Tracker
A simple React Native implementation for tracking water daily.


**Install packages:**
```
$ yarn install
$ cd ios && pod install && cd ..
```

**Run dev:**
```
$ yarn ios
$ yarn android
```
